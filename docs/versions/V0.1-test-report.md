# V0.1 测试报告

- 日期：2026-09-08
- 范围：本地构建、九个页面、资源与链接、响应式、主要交互、仓库子目录托管。
- 结论：本地功能验收通过。用户选择先本地验收、保留两套部署配置；公网发布不属于本次执行范围，不计为已通过的上线验证。

## 自动化校验

命令：`npm run check`

| 检查 | 结果 |
| --- | --- |
| 九个页面标题唯一、单一 H1、中文语言和元信息 | 通过 |
| 全部内部链接、脚本、图片、CSS 和图标引用 | 通过 |
| 外链安全属性、无虚构简历下载及受限仓库直链 | 通过 |
| 图片和脚本体积预算 | 通过 |
| HTML 特殊字符转义 | 通过 |
| HTTP 路由、子目录部署、跳转、404、请求方法与路径限制 | 通过 |

## 浏览器回归

使用已有 Playwright 1.62.1 驱动 Microsoft Edge 152.0.4191.66。逐页面结果保存在 `V0.1-browser-results.json`。桌面和手机截图分别归档为 `V0.1-home-desktop.webp`、`V0.1-home-mobile.webp`。

| 视口 | 页面数 | 横向溢出 | 标题/按钮文字溢出 | 图片加载 |
| --- | --- | --- | --- | --- |
| 1440 × 1000 | 9 | 无 | 无 | 正常 |
| 1920 × 1080 | 9 | 无 | 无 | 正常 |
| 768 × 1024 | 9 | 无 | 无 | 正常 |
| 390 × 844 | 9 | 无 | 无 | 正常 |
| 320 × 740 | 9 | 无 | 无 | 正常 |

45 组页面与视口组合通过。浏览器脚本错误与失败资源请求均为 0。桌面及手机全页截图经人工查看，修正了项目缩略图的固定高度导致的裁切。

## 交互覆盖

1. 移动导航打开、Escape 关闭与页面跳转。
2. 全部 / AI / Web 项目筛选及结果计数。
3. 笔记关键词搜索、分类筛选、空状态、重置与文章跳转。
4. 未提供正式简历时，下载按钮保持禁用且没有失效文件链接。
5. 任意深层不存在地址返回 404，可返回首页；返回顶部恢复滚动和焦点。
6. 禁用 JavaScript 后，桌面核心内容与导航仍然可用。

## 资源和外链

- 首页照片与网站真实缩略图均已本地化，生产页面不请求外部图片或字体。
- GitHub 个人主页为公开账号 `YXY614-ASH`，已通过 GitHub 用户接口核验。
- 指定仓库通过 SSH 可访问；匿名仓库接口 404，因此未在公开页面提供需授权的源码直链。
- 自动测试不把缺失 Demo、PDF 或未上线公网域名当作有效链接。

## 尚未验收

- GitHub Pages / Vercel 的真实线上发布与公网访问。
- Safari、Firefox 及实体手机上的浏览器行为。
- 真实 PDF 下载和 AI Demo，待用户提供资料后再加入回归。
