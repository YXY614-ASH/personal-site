# V0.3 测试报告

- 日期：2026-09-09
- 状态：本地接入验收通过。
- 范围：作品集项目页、chatbot 本地启动器、PDF 检索、计算器、无密钥行为与旧项目回归。

## 结果

| 检查 | 结果 |
| --- | --- |
| 个人网站 `npm run check` | 8 项通过，0 项失败 |
| 个人网站浏览器回归 | 45 组页面/视口、7 组交互，0 脚本错误 |
| chatbot 接入测试 | 6 项通过 |
| chatbot 原有测试 | 15 项通过 |
| Python 语法 / 本地导入 | 通过，使用 Python 3.12.10 |
| 本地 Demo | `http://127.0.0.1:7861/` 返回 200，监听本机 |
| 模型联网 | 未执行；未配置新密钥时只测试本地工具 |

## 安全检查

- 没有读取或复用旧 `config.py` 中暴露的 API key。
- 接入启动器使用 `DEEPSEEK_API_KEY` 环境变量；不提供 `--share` 参数。
- 网站显示“本机 Demo”和“模型未连接”状态，没有声称在线可用。
- chatbot 原仓库未修改；仅在个人网站仓库加入启动器、fixture、测试和说明。

## 已知限制

需要用户配置新 DeepSeek key 才能验收真实模型生成；没有真实 PDF 课程资料时只使用测试 fixture 验证检索路径。Demo 仅本机访问，尚未部署 Gradio 到公网。
